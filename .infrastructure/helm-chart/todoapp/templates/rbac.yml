kind: ServiceAccount
apiVersion: v1
metadata:
  name: {{ .Values.todoapp.serviceAccountName.name }}
  namespace: mateapp

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: mateapp
  name: {{ .Values.todoapp.serviceAccountName.name }}
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "watch", "list"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Values.todoapp.serviceAccountName.name }}-binding
  namespace: mateapp
subjects:
- kind: ServiceAccount
  name: {{ .Values.todoapp.serviceAccountName.name }}
roleRef:
  kind: Role
  name: {{ .Values.todoapp.serviceAccountName.name }}
  apiGroup: rbac.authorization.k8s.io